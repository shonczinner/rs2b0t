export const QuestFood = { name: 'Trout' as string | null };

/** Why: 8 unless a quest proves it needs more; the old 12-to-20 packs spent most of the inventory on food that was never eaten. */
export const FOOD_FLOAT = 8;
